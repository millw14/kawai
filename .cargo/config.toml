# Cargo configuration for cross-platform builds

# For cross-compiling to Linux from Windows:
# [target.x86_64-unknown-linux-gnu]
# linker = "x86_64-linux-gnu-gcc"

# Build configuration
[build]
# Uncomment to use a specific target by default
# target = "x86_64-unknown-linux-gnu"


